# 需求?

帮我给 src\HttpServer\cHttpServerRequest.cls? 增加一个 Files 成员，能够接收来自前端上传的文件，解析到多个（包括1个）文件到一个结构体数组里面，结构体包含上传文件所需，表单字段则到 Form 里面，当然 url 键值对也是照旧解析的。

开发者在 ctx.request.files("xxx") 就可以进行文件数据操作，

同时实现分片传输机制（你来定一个规范，让前端执行就行）

注意，多个文件域可能包含多个文件，你会怎么设计？
