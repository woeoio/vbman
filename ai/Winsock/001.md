# 需求

增加一个绑定user的函数：BindUser(byval User as variant, Client As cWinsock)，使用一个内部字典绑定当前实例到user键，提供检查是否存在和删除操作函数。哦，还有提供一个发送数据的函数 SendToUser(byval User as variant)。

再增加一个绑定组的函数：BindGroup(byval Group as String, Client As cWinsock)，
逻辑和上面的绑定用户一致,但应该增加一些关于组的更多函数，比如说能够获取组组内成员用户名数组,绑定组名之前必须绑定用户，这是前提.然后要注意刚才遇到过的一些问题，不要重复出现

【已实现】
